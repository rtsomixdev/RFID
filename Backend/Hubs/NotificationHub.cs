using Microsoft.AspNetCore.SignalR;

namespace Backend.Hubs
{
    // ท่อสำหรับตะโกนบอก Frontend
    public class NotificationHub : Hub
    {
        // ฟังก์ชันนี้ Frontend เรียกใช้ได้ (ถ้าจำเป็น)
    }
}